#include <stdio.h>

/**
 * main: Entry point
 *
 * Return: Always 0
 */

/** prints alphabet in lowercase **/

int main(){
	char ch = 'a';
	for (ch = 'a'; ch <= 'z'; ch++)
		putchar (ch);
	return (0);
}
