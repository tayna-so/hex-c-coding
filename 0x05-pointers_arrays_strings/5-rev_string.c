/** File: 5-rev_string.c  Auth: Tayna Sousa **/
#include "holberton.h"

/**
 * rev_string - it reverses a string
 * @s: string to be printed
 */

void rev_string(char *s)
{
	int p = 0;

	if (s[c] != '\0')
	{
		_putchar("%p", s[p]);
		p++;
	while (s[p] != '\0');
	}
}
